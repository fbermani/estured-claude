// Stub de "server-only" para Vitest — ese paquete solo tiene sentido
// bajo el resolver especial de Next (client vs. server components); en
// tests de Node plano no aplica esa distinción, así que se neutraliza.
export {};
